{
  "name": "minigame-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r dev",
    "dev:api": "pnpm --filter api start:dev",
    "dev:admin": "pnpm --filter soybean-admin dev",
    "build:api": "pnpm --filter api build"
  },
  "devDependencies": {
    "@types/connect": "^3.4.38",
    "@types/http-errors": "^2.0.5",
    "@types/range-parser": "^1.2.7",
    "@types/send": "^1.2.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "pg": "^8.18.0"
  }
}
